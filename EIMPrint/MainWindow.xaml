<Window x:Class="EIMPrint.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="MainWindow" Height="350" Width="525" DataContext="{Binding Source={StaticResource Locator}, Path=Main}">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
        </Grid.RowDefinitions>
        <WrapPanel Grid.Row="0" Orientation="Horizontal">
            <Image Source="Images/Female.png" Stretch="None"></Image>
            <Image Source="Images/Male.png" Stretch="None"></Image>
        </WrapPanel>
        <Button Grid.Row="1" Content="Print" Command="{Binding PrintCommand}"></Button>
    </Grid>
</Window>
